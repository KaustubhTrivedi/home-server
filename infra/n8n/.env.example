# n8n Environment Configuration
# Copy this file to .env and modify the values according to your setup

# DOMAIN_NAME and SUBDOMAIN together determine where n8n will be reachable from
# The top level domain to serve from
DOMAIN_NAME=example.com

# The subdomain to serve from
SUBDOMAIN=n8n

# The above example serve n8n at: https://n8n.example.com

# Optional timezone to set which gets used by Cron and other scheduling nodes
# New York is the default value if not set
GENERIC_TIMEZONE=America/New_York

# n8n Configuration
# Basic authentication (set if you want to protect n8n with basic auth)
# N8N_BASIC_AUTH_ACTIVE=true
# N8N_BASIC_AUTH_USER=admin
# N8N_BASIC_AUTH_PASSWORD=your_secure_password_here

# Webhook URL (usually automatic but can be set manually)
# WEBHOOK_URL=https://n8n.example.com/

# Encryption key for credentials (generate a random 32-character string)
# N8N_ENCRYPTION_KEY=your_32_character_encryption_key_here

# Default credentials folder
# N8N_USER_FOLDER=/home/node/.n8n

# Maximum payload size for requests (in MB)
# N8N_PAYLOAD_SIZE_MAX=16

# Execution timeout (in seconds)
# EXECUTIONS_TIMEOUT=3600

# Execution mode (regular or queue)
# EXECUTIONS_MODE=regular

# Log level (error, warn, info, verbose, debug, silly)
# N8N_LOG_LEVEL=info

# Security settings
# Disable metrics endpoint
# N8N_METRICS=false

# User management settings (for enterprise features)
# N8N_USER_MANAGEMENT_DISABLED=false

# SMTP settings for email notifications (optional)
# N8N_SMTP_HOST=smtp.gmail.com
# N8N_SMTP_PORT=587
# N8N_SMTP_USER=your-email@gmail.com
# N8N_SMTP_PASS=your-app-password
# N8N_SMTP_SENDER=your-email@gmail.com
# N8N_SMTP_SSL=false
